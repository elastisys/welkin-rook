# Upgrade rook-ceph from v1.14.x to v1.18.x

## Prerequisites

- [ ] Make sure that you are running ceph version Reef (v18) or later
- [ ] Check the state of rook-ceph so it is healthy before you begin:

    ```bash
    helm -n rook-ceph list --all
    # should show all deployed
    kubectl -n rook-ceph get jobs
    # should show all completed
    kubectl -n rook-ceph get pods
    # should show all running or completed
    kubectl -n rook-ceph get cephclusters
    # should show PHASE = Ready and HEALTH = HEALTH_OK
    # If the status is not OK, you can extract some more information with the commands below.
    kubectl exec -n rook-ceph deploy/rook-ceph-toolbox -- ceph health detail
    kubectl exec -n rook-ceph deploy/rook-ceph-toolbox -- ceph status
    ```

## Apply upgrade to 1.15.x - _disruptive_

- 1. Set rook: major: 15 inside `CK8S_CONFIG_PATH/config/rook/values.yaml`:

    ```console
    commons:
      rook:
        major: 1.15
    ```

    To give time for the operator to update internal objects, such as CRDs.
    We run the upgrade in two steps.

- 1. Upgrade rook-ceph operator:

    ```console
    helmfile -e workload -l app=operator diff
    helmfile -e workload -l app=operator apply
    ```

- 1. Upgrade rook-ceph cluster

    Wait a short while for the operator, before upgrading the cluster.

    ```console
    helmfile -e workload -l app=cluster diff
    helmfile -e workload -l app=cluster apply
    ```

- 1. [Postrequisites](#postrequisites)

## Apply upgrade to 1.16.x - _disruptive_


- 1. Set rook: major: 16 inside `CK8S_CONFIG_PATH/config/rook/values.yaml`:

    ```console
    commons:
      rook:
        major: 1.16
    ```

- 1. Upgrade rook-ceph operator:

    ```console
    helmfile -e workload -l app=operator diff
    helmfile -e workload -l app=operator apply
    ```

- 1. Upgrade rook-ceph cluster

    Wait a short while for the operator, before upgrading the cluster.

    ```console
    helmfile -e workload -l app=cluster diff
    helmfile -e workload -l app=cluster apply
    ```

- 1. [Postrequisites](#postrequisites)

## Apply upgrade to 1.17.x - _disruptive_

- 1. Set rook: major: 17 inside `CK8S_CONFIG_PATH/config/rook/values.yaml`:

    ```console
    commons:
      rook:
        major: 1.17
    ```

- 1. Upgrade rook-ceph:

    To give time for the operator to update internal objects, such as CRDs.
    We run the upgrade in two steps.

- 1. Upgrade rook-ceph operator:

    ```console
    helmfile -e workload -l app=operator diff
    helmfile -e workload -l app=operator apply
    ```

- 1. Upgrade rook-ceph cluster

    Wait a short while for the operator, before upgrading the cluster.

    ```console
    helmfile -e workload -l app=cluster diff
    helmfile -e workload -l app=cluster apply
    ```

- 1. [Postrequisites](#postrequisites)

## Apply upgrade to 1.18.x - _disruptive_

> [!WARNING]
> This upgrade will replace the provisioner and plugin pods with ceph csi operator
> managed pods, which means that these existing pods will be deleted, and new pods will
> spawn, which requires extra attention to network policies and pod security policies
> when upgrading

- 1. Set rook: major: 18 inside `CK8S_CONFIG_PATH/config/rook/values.yaml`:

    ```console
    commons:
      rook:
        major: 1.18
    ```

- 1. Upgrade rook-ceph:

    To give time for the operator to update internal objects, such as CRDs.
    We run the upgrade in two steps.

- 1. Upgrade rook-ceph operator:

    ```console
    helmfile -e workload -l app=operator diff
    helmfile -e workload -l app=operator apply
    ```

- 1. Upgrade rook-ceph cluster

    Wait a short while for the operator, before upgrading the cluster.

    ```console
    helmfile -e workload -l app=cluster diff
    helmfile -e workload -l app=cluster apply
    ```

- 1. [Postrequisites](#postrequisites)

## Postrequisites

- [ ] Check the state of rook-ceph so it is healthy before you end:

    ```bash
    helm -n rook-ceph list --all
    # should show all deployed
    kubectl -n rook-ceph get jobs
    # should show all completed
    kubectl -n rook-ceph get pods
    # should show all running or completed
    kubectl -n rook-ceph get cephclusters
    # should show PHASE = Ready and HEALTH = HEALTH_OK
    ```

- [ ] Run the test script

    ```bash
    ./scripts/test-rook.sh wc
    ./scripts/test-rook.sh sc
    ```
